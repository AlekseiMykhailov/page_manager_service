const { applyForm } = require('./applyForm');
const { benefits } = require('./benefits');
const { header } = require('./header');
const { reviews } = require('./reviews');
const { instructors } = require('./instructors');

module.exports = {
  applyForm,
  benefits,
  instructors,
  header,
  reviews,
};
